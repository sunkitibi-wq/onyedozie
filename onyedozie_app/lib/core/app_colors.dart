import 'package:flutter/material.dart';

/// Centralized color constants for the Onyendozi Connect app.
/// Blue / Orange modern theme inspired by Monex design system.
class AppColors {
  AppColors._();

  // ── Primary Blue ──────────────────────────────────────────────
  static const Color primary = Color(0xFF4361EE);
  static const Color primaryDark = Color(0xFF3451DB);
  static const Color primaryNavy = Color(0xFF1A237E);
  static const Color primaryLight = Color(0xFFBBD4FF);

  // ── Backgrounds ───────────────────────────────────────────────
  static const Color pageBg = Color(0xFFF0F4FF);
  static const Color subtleBg = Color(0xFFE8EDFF);
  static const Color sectionBg = Color(0xFFEDF0FF);
  static const Color cardBg = Colors.white;

  // ── Borders ───────────────────────────────────────────────────
  static const Color border = Color(0xFFCED4E0);

  // ── Text ──────────────────────────────────────────────────────
  static const Color heading = Color(0xFF1E293B);
  static const Color secondaryText = Color(0xFF64748B);
  static const Color tertiaryText = Color(0xFF94A3B8);
  static const Color hintText = Color(0xFFB0B8C9);

  // ── Accent (Orange) ───────────────────────────────────────────
  static const Color accent = Color(0xFFFF8C00);
  static const Color accentDark = Color(0xFFE65100);
  static const Color accentLight = Color(0xFFFFF3E0);
  static const Color accentText = Color(0xFFF57C00);
  static const Color accentWarm = Color(0xFFBF360C);

  // ── Misc ──────────────────────────────────────────────────────
  static const Color progressTrack = Color(0xFFDDE4F0);
  static const Color unreadBg = Color(0xFFEFF3FF);
  static const Color lightTextOnDark = Color(0xFFB0C4FF);
  static const Color gradientEnd = Color(0xFF2D3FBE);
  static const Color mapPlaceholder = Color(0xFFE0E5F5);
}
